# Backend

This repository uses Next.js (full‑stack), so the API routes and database logic currently live inside the Next.js project under `frontend/app/api` and `frontend/lib`.

This `backend/` folder is reserved for a future standalone backend service if you decide to fully separate the API from the Next.js app.

